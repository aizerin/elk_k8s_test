{% import 'macros.j2' as macros with context %}

input {
    kafka {
        {{ macros.kafka_input() | indent(8) }}
        codec => "json"
        session_timeout_ms => "60000"
        heartbeat_interval_ms => "10000"
    }
}
